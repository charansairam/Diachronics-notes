import type { QuartzTransformerPlugin } from "@quartz-community/types";

declare const LinguisticNotation: QuartzTransformerPlugin;

export default LinguisticNotation;
export declare const manifest: {
  name: string;
  displayName: string;
  description: string;
  version: string;
  quartzVersion: string;
  category: string;
  defaultOrder: number;
};
